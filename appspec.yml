version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/ks-chicken-server
    overwrite: true

hooks:
  BeforeInstall:
    - location: scripts/clean.sh
      runas: root
  AfterInstall:
    - location: scripts/install_dependencies.sh
      runas: root
